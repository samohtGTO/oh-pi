# Analytics Dashboard Environment Configuration
#
# API_MODE:
#   "mock" — Uses generated mock data (default, no server needed)
#   "api"  — Fetches from the Express API server at API_BASE
#
VITE_API_MODE=mock
VITE_API_BASE=http://localhost:31415