{
	"name": "@ifi/pi-analytics-db",
	"version": "0.5.0",
	"description": "SQLite database layer for Pi analytics with migrations",
	"files": [
		"src",
		"migrations"
	],
	"type": "module",
	"main": "./src/index.ts",
	"exports": {
		".": "./src/index.ts",
		"./schema": "./src/schema.ts",
		"./db": "./src/db.ts",
		"./migrations": "./src/migrations.ts"
	},
	"scripts": {
		"build": "echo 'No compilation needed - source is TypeScript'",
		"build:fast": "echo 'No compilation needed - source is TypeScript'",
		"typecheck": "tsgo --noEmit",
		"dev": "tsgo --build --watch"
	},
	"dependencies": {
		"better-sqlite3": "^12.0.0",
		"drizzle-orm": ">=0.45.2"
	},
	"devDependencies": {
		"@types/better-sqlite3": "^7.6.13",
		"drizzle-kit": "^0.31.10"
	}
}
